# Image_Filters
Image Filters (grayscale, reflect, blur, and edge detection). 
C code to take input and filter selection through the Command-Line, 
access each pixel value of the .bmp image, create a copy, and apply the filter as selected by changing the pixel values of each pixel of the image to output a final filtered image (.bmp) in the name and directory specified in the CLI at start.
